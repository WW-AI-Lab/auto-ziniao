# @ww-ai-lab/auto-ziniao

Auto Ziniao CLI. 安装后提供 `auto-ziniao` 命令，用于校验、运行和管理基于 ZClaw bridge 的店铺操作 flow。

```bash
npm install -g @ww-ai-lab/auto-ziniao
auto-ziniao --help
```

浏览器操作只能通过紫鸟 ZClaw bridge 执行；本包不提供本机浏览器自动化 fallback。
