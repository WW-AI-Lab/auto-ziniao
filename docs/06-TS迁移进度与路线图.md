# TypeScript 迁移进度与路线图

> 状态日期：2026-06-15  
> 目标：用一个专门文件跟踪 TS 迁移当前状态、已完成边界、下一步 OpenSpec change 和剩余工作。长期架构设计仍以 `docs/05-TS重构技术蓝图.md` 为准。

## 当前结论

当前迁移已完成 **M1：契约与 TS 骨架**、**M2：安全出口可用**、**M3：Flow Engine 迁移**、**M4：Self-Heal 迁移**、**M5：CLI 迁移**、**M6：WebAdmin 后端 TS 化**、**WebAdmin 前端整理与双跑切换准备** 和 **M7：Python 依赖清零**。生产入口为 `ziniao` CLI（`pnpm ziniao ...`）；TypeScript 已全面覆盖 flow 加载、静态校验、离线执行语义、ZClaw tool dispatch、output/run log 写入、self-heal 的错误分类、known issues、prompt/context 生成、cooldown、Agent CLI adapter、dry-run，`apps/api` 的 Fastify WebAdmin API 能力，以及 `apps/web` 的 React 前端构建能力。Python 运行时（`engine/`、`manager.py`、`.venv`、`__pycache__`）已从 active tree 删除，仅可通过 git 历史恢复。

当前主规格：

- `flow-contract-schemas`
- `flow-compatibility-baseline`
- `typescript-workspace-baseline`
- `typescript-zclaw-client`
- `typescript-flow-engine`
- `typescript-self-heal`
- `typescript-cli`
- `typescript-webadmin-api`
- `typescript-webadmin-frontend`

当前 TS 包：

- `packages/core`
- `packages/schemas`
- `packages/zclaw`
- `packages/flow-engine`
- `packages/self-heal`
- `packages/cli`
- `apps/api`
- `apps/web`

当前活动 TS 迁移 change：

- `add-typescript-flow-pacing-policy`：TS flow-engine 操作节奏策略提案；不迁移 CLI，不改变 Python 生产入口。
- `remove-python-runtime`：M7 Python 依赖清零；继续实施前必须先复核本文件中的 WebAdmin 前端整理与双跑切换准备证据。

已归档 TS 迁移前置 change：

- `openspec/changes/archive/2026-06-15-prepare-webadmin-frontend-cutover`：WebAdmin 前端整理、API 类型复用、TS WebAdmin 静态托管和 M7 preflight 证据补齐。

## 已完成

### M1：契约与 TS 骨架

已由 OpenSpec change `establish-typescript-contract-baseline` 落地：

- 根级 `pnpm workspace`
- `packages/core`
- `packages/schemas`
- flow definition schema
- runtime data schema
- WebAdmin DTO schema
- JSON Schema export
- current flow golden summary
- negative fixtures
- `pnpm validate:baseline`
- 静态安全扫描

验收状态：

- 当前 `flows/*.json` 可被 TS schema 解析。
- TS baseline 测试覆盖所有流程的静态校验。
- 默认 TS baseline 不执行真实 flow，不调用真实 ZClaw bridge。

### M2：安全出口可用

已由 OpenSpec change `add-typescript-zclaw-client` 落地：

- `packages/zclaw`
- `ZCLAW_API_KEY` / `ZCLAW_BASE_URL` / `~/.zclaw/config.json` 配置读取
- `/zclaw/*` path 白名单
- `GET /zclaw/tools`
- `POST /zclaw/tools/invoke`
- `listStores()`、`openStore()`、`closeStore()`、`visit()`、`executeScript()`、`screenshot()`、`click()`、`waitElement()`
- ZClaw 结构化错误分类
- 离线 Vitest 单测
- 安全扫描 allowlist：只有 `packages/zclaw` 可直接访问 bridge

验收状态：

- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `pnpm validate:baseline`
- `pnpm security:scan`
- `python3 manager.py list`
- `python3 manager.py validate orders_overview`
- `openspec validate --specs --strict`

### M3：Flow Engine 迁移

已由 OpenSpec change `add-typescript-flow-engine` 落地：

- `packages/flow-engine`
- `loadFlow(flowId | filePath)`
- `validateFlow()` wrapper，复用 `packages/schemas.validateFlowContract()`
- `parseParams(pairs)`
- `FlowRunner` / `runFlow()`
- 参数合并、`${params.x}`、`${step_save.path}`、`${date}`、`${datetime}`、`${timestamp}`
- condition、branch、goto、`on_fail.branch`、`end` 和最大执行次数保护
- validate：`path`、`not_empty`、`min_rows`、`require_fields`、`contains`
- action：`sleep`、`print`、`assert`、`fail`、`save_json`、`save_csv`、`close_store`
- `FlowToolClient` interface 与 `packages/zclaw` adapter
- 特殊工具映射：`list_stores`、`open_store`、`visit_page`、`execute_script`、`click_element`、`take_screenshot`、`wait_for_element`、`close_store`
- `@extracts/*.js` 读取和变量替换；Node 只传递脚本文本，不执行 extract JS
- `data/logs/runs.jsonl` schema-compatible append
- 当前五个 flow 的离线语义/golden 测试

验收状态：

- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `pnpm validate:baseline`
- `pnpm security:scan`
- 精确边界检查：`rg -n "9481|/zclaw/tools|/zclaw/" packages/flow-engine/src` 无命中
- `python3 manager.py list`
- `python3 manager.py validate orders_overview`

本地双跑记录：

| 类型 | 命令 | 状态 | 摘要 |
|------|------|------|------|
| Python | `python3 manager.py run webadmin_selftest -p greeting=m3-double-run --no-heal` | success | 不依赖 bridge，写入真实 `data/logs/runs.jsonl`，耗时约 10010ms |
| TS | `pnpm vitest run packages/flow-engine/src/index.test.ts -t "supports a local-only double-run record for M3"` | success | mock client、临时 data root、no-op sleeper，1 passed |

真实 bridge flow 双跑状态：未执行。原因是本次没有用户明确确认紫鸟客户端在线、测试店铺参数和可执行的低风险真实 flow；按安全规则不擅自打开店铺浏览器，也不改用本机浏览器。

### M4：Self-Heal 迁移

已由 OpenSpec change `add-typescript-self-heal` 落地：

- `packages/self-heal`
- `classifyError()`：兼容 Python 自愈错误分类
- `checkKnownIssues()`：读取 `learnings/known_issues.json`，支持 `resolved: true`、`pattern` 子串和 `flow_id + step_id` 命中
- `buildHealPrompt()`：生成 `data/logs/heals/<heal_id>.json` 和 `data/logs/heals/<heal_id>_prompt.md`
- `heal_templates/` 模板优先级和 safe placeholder rendering
- `loadHealConfig()`：兼容 Python 默认配置与 `config.json` 浅合并，只读不写
- `checkCooldown()` 与 `learnings/heals.jsonl` 事件写入
- `AgentRunner` / `createCommandAgentRunner()`：支持 command placeholder、timeout、non-zero exit、command missing
- `triggerHeal()`：支持 dry-run、disabled、known issue skip、cooldown skip、mock runner trigger
- `buildTriggerInputFromFailure()`：从 M3 flow-engine failure metadata 组装 self-heal 输入
- `packages/core` 新增 JSON/JSONL 写入 helper
- `packages/schemas` 扩展 heal context/event 兼容字段
- 安全扫描新增 `packages/self-heal` 禁止依赖/import `@ziniao/zclaw` 的规则

验收状态：

- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `pnpm security:scan`
- `pnpm validate:baseline`
- 精确边界检查：`rg -n "9481|/zclaw/tools|/zclaw/" packages/self-heal/src` 无命中
- `python3 manager.py list`
- `python3 manager.py validate orders_overview`
- `openspec validate add-typescript-self-heal --strict`

M4 dry-run 边界：

- dry-run 会生成 `data/logs/heals/<heal_id>.json` 与 `data/logs/heals/<heal_id>_prompt.md`。
- dry-run 不调用真实 Agent CLI，不消耗 cooldown quota，不追加 `triggered` 事件。
- 默认 baseline 使用 mock `AgentRunner`、临时 data root 和固定 clock，不调用 OpenClaw/Claude/Cursor。

M4 回滚验证记录：

- 等价回滚检查：`git diff --name-only -- manager.py engine webadmin` 无输出，确认 Python 生产入口、engine 与 WebAdmin 未被 M4 修改。
- 回滚方式：删除或隔离 `packages/self-heal`，恢复 root `package.json`、`tsconfig.base.json`、`vitest.config.ts` 与 `scripts/security-scan.ts` 中的 M4 改动。
- 回滚后基线：`python3 manager.py list` 与 `python3 manager.py validate orders_overview` 已通过。

### M5：CLI 迁移

已由 OpenSpec change `add-typescript-cli` 落地：

- `packages/cli`
- workspace package `@ziniao/cli`
- bin 入口 `ziniao`
- CLI 框架 `commander`
- 命令：`list`、`run`、`run-all`、`retry`、`new`、`validate`、`history`、`enable`、`disable`、`heals`、`stats`、`cron`
- `--repo-root` / `--data-root` 测试注入入口
- `run` 支持 `-p/--param`、`-v/--verbose`、`--no-heal`、`--heal-dry-run`
- 通过 `packages/flow-engine` 执行 flow，不直接访问 bridge
- 通过 `packages/self-heal` 触发自愈，不重复实现 prompt/cooldown/known issues 逻辑
- CLI 离线 Vitest 覆盖 flow 管理、运行、自愈 mock/dry-run、history/heals/stats/cron
- 安全扫描扩展：`packages/cli` 禁止 direct bridge 访问、ZClaw API key 读取、`@ziniao/zclaw` 直接依赖、浏览器自动化依赖

验收状态：

- `pnpm --filter @ziniao/cli typecheck`
- `pnpm vitest run packages/cli/src/index.test.ts`
- `pnpm security:scan`
- 精确边界检查：`rg -n "9481|/zclaw/tools|/zclaw/" packages/cli/src` 无命中

M5 边界：

- `ziniao ...` 是生产 CLI 入口（M7 后唯一入口）。
- 如果 `add-typescript-flow-pacing-policy` 后续落地，CLI 只消费 flow-engine 暴露的 pacing 能力，不在 CLI 内重复实现 pacing。

### M6：WebAdmin 后端 TS 化

已由 OpenSpec change `add-typescript-webadmin-api` 落地：

- `apps/api`（原 `apps/webadmin-api`）
- workspace package `@ziniao/api`（原 `@ziniao/webadmin-api`）
- Fastify app factory 与启动入口，默认仅监听 `127.0.0.1`
- REST API：flows 列表/详情/validate/save/manual run/status
- monitoring API：runs、heals、known issues、stats、output 浏览/预览/下载
- schedules API：CRUD、enable/disable、`interval` / `daily` / 五字段 `cron`、调度历史
- in-process scheduler、flow-level lock、missed trigger 不补跑
- chat API：agents、sessions、messages、SSE send，默认测试使用 mock `AgentRunner`
- 静态托管 `apps/web/dist`，缺失 dist 时 API 仍可用
- `packages/schemas` 补齐 WebAdmin API DTO 与 `webadmin-api.schema.json`
- 安全扫描扩展到 `apps/*`，阻断 direct bridge、ZClaw API key、本机浏览器和浏览器自动化依赖

SQLite 选型记录：

- design 优先评估 `better-sqlite3`。
- 当前 pnpm 安装策略忽略 native build script，导致 `better-sqlite3` binding 不可用。
- M6 按 design 允许的替代方案改用 Node 24 内置 `node:sqlite`，保持同步 SQLite 访问模型；运行测试会出现 Node 的 experimental warning，但 baseline 通过。

验收状态：

- `pnpm --filter @ziniao/api typecheck`
- `pnpm --filter @ziniao/api test`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
- `pnpm schemas:export -- --check`
- `pnpm security:scan`
- `pnpm validate:baseline`
- 精确边界检查：`rg -n "9481|/zclaw/tools|ZCLAW_API_KEY|\\.zclaw" apps/api/src` 无命中
- `python3 manager.py list`
- `python3 manager.py validate orders_overview`

M6 边界：

- `apps/api` 是 TS WebAdmin 后端（M7 后唯一后端）。
- `apps/web` 是 TS WebAdmin 前端。
- WebAdmin API 不直接访问 ZClaw bridge，不读取 ZClaw API key，不打开本机浏览器。
- 默认 baseline 只使用离线 mock、临时 data root、固定 clock 和 no-op sleeper，不执行真实 flow，不调用真实 Agent CLI。

### WebAdmin 前端整理与双跑切换准备

由 OpenSpec change `prepare-webadmin-frontend-cutover` 落地，用于解除 `remove-python-runtime` 的 M7 preflight 阻塞：

- 新增 `apps/web`（原 `apps/webadmin-frontend`），迁移旧 `webadmin/frontend` 的 React + Vite + TypeScript + antd + `@ant-design/x` 前端源码、assets 和构建配置。
- package name 为 `@ziniao/web`（原 `@ziniao/webadmin-frontend`），纳入 root `pnpm build`、`pnpm typecheck` 和 `pnpm validate:baseline`。
- 前端 API client 改为复用 `@ziniao/schemas/webadmin` WebAdmin DTO 类型；`packages/schemas` 同步补齐 `WebAdminStats`、`HealEntry`、`HealDetail`、`ChatAgentInfo`、`ChatToolCall`、`OutputPreview` 和 chat SSE 扩展事件。
- `apps/api` 默认静态托管路径切到 `apps/web/dist`，保留显式 `frontendDist` 注入能力和 missing dist 结构化响应。
- `/api/stats` 补齐前端需要的 `runs_error`、`heals_total`、`chat_sessions` 和 per-flow `heal_count` 字段。

本阶段证据：

| 类型 | 命令 | 状态 | 摘要 |
|------|------|------|------|
| 前端构建 | `pnpm --filter @ziniao/web build` | success | 生成 `apps/web/dist/index.html` 和 assets；Vite build 通过 |
| WebAdmin API test/smoke | `pnpm --filter @ziniao/api test` | success | 2 个 test files、10 个 tests；覆盖 `/api/flows`、`/api/stats`、chat SSE mock、static missing/present dist 和显式 `frontendDist` 注入 |
| API/前端边界 | `rg -n "9481|/zclaw/tools|ZCLAW_API_KEY|\\.zclaw" apps/api/src apps/web/src` | success | 无命中 |
| TS CLI validate | `pnpm ziniao validate orders_overview` | success | `orders_overview 校验通过` |
| TS CLI local-only run | `pnpm ziniao run webadmin_selftest -p greeting=frontend-cutover --no-heal` | success | `data/logs/runs.jsonl` 记录 `2026-06-15 10:55:46`，`webadmin_selftest` 参数 `greeting=frontend-cutover`，输出 `selftest: frontend-cutover` / `selftest done` |

真实 bridge flow 双跑状态：未执行。原因是本次没有用户明确确认紫鸟客户端在线、测试店铺参数和可执行的低风险真实 flow；按最高安全规则不擅自打开店铺浏览器，也不改用本机浏览器或 Playwright/Selenium/Puppeteer/browser-use。

本阶段边界：

- 已完成前端 cutover 准备，为 M7 Python 清零提供完整的 WebAdmin 前端支撑。

## 当前边界（M7 完成后）

当前生产路径已全面切换到 TS：

- `pnpm ziniao ...` / `ziniao ...` 是唯一 CLI 入口
- 真实 flow 执行由 `packages/flow-engine` 完成
- 自愈由 `packages/self-heal` 完成
- WebAdmin 后端为 `apps/api`（Fastify + SQLite）
- WebAdmin 前端为 `apps/web`（React + Vite）

当前禁止事项：

- 不得绕过 `packages/zclaw` 直接访问 ZClaw bridge。
- 不得引入 Playwright/Selenium/Puppeteer/browser-use。
- 不得在 baseline 验证中执行真实 flow。
- 不得在 baseline 验证中调用真实 `POST /zclaw/tools/invoke`。
- 不得在 baseline 验证中调用真实 OpenClaw/Claude/Cursor Agent CLI。

## M7 完成记录：Python 依赖清零

M7 已完成：

- 移除 `engine/` 目录（Python 引擎：`flow_engine.py`、`self_heal.py`、`manager.py`、`paths.py`、`zclaw_client.py`）
- 移除 `manager.py` 根入口
- 清理 `__pycache__/`、`.venv/`、`.pytest_cache/` 缓存
- 更新 `.gitignore` 移除 Python 相关规则
- 更新所有文档中的 Python 命令引用为 TS CLI 命令
- 安全扫描通过：无 tracked `.py` 文件或 `requirements.txt`
- 全量验证通过：typecheck + 92 tests + build + schemas:export + security:scan

Python 历史实现可通过 git 历史恢复（恢复点：`68d7db8a`，branch `develop`）。

## 后续路线图

1. **M3：核心 Flow Engine 可替代（已完成）**
   - `packages/flow-engine`
   - `ziniao validate/run` 的底层能力准备
   - 低风险 flow 真机双跑

2. **M4：自愈链路可替代（已完成）**
   - `packages/self-heal`
   - known issues
   - prompt rendering
   - cooldown
   - Agent CLI / OpenClaw adapter
   - dry-run

3. **M5：CLI 可替代（已完成）**
   - `packages/cli`
   - `ziniao ...`
   - 兼容当前 `manager.py` 命令语义
   - 过渡入口已可用于后续 Python/TS 双跑切换

4. **M6：WebAdmin 后端 TS 化（已完成）**
   - `apps/api`（原 `apps/webadmin-api`）
   - Fastify + SQLite
   - REST/SSE
   - 调度器
   - 托管前端 dist

5. **WebAdmin 前端整理与双跑切换准备（已完成）**
   - `apps/web`（原 `apps/webadmin-frontend`）
   - 复用 `packages/schemas` API 类型
   - TS WebAdmin API static serving smoke
   - M7 preflight 证据记录

6. **M7：Python 依赖清零（已完成）**
   - 移除 Python engine（`engine/`、`manager.py`）
   - 删除 FastAPI WebAdmin 后端
   - 清理 Python 缓存（`__pycache__`、`.venv`、`.pytest_cache`）
   - 文档、AGENTS.md、skill 全部切换到 TS 命令
   - 安全扫描确认无 Python 回归

## 回滚原则

- M1-M7 均已完成，Python 运行时已从 active tree 清零。
- 如需回滚到 Python 版本，可通过 git 历史恢复（恢复点：`68d7db8a`，branch `develop`）。
- 回滚后必须验证：

```bash
pnpm ziniao list
pnpm ziniao validate orders_overview
```
