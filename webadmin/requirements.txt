# webadmin 子工程独立依赖（引擎主体保持零第三方依赖，勿在引擎模块中 import）
# 安装: python3 -m venv .venv && .venv/bin/pip install -r webadmin/requirements.txt
fastapi==0.136.3
uvicorn[standard]==0.49.0

# 测试（可选）
pytest==9.0.3
httpx==0.28.1
